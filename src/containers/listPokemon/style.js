import styled from "styled-components";

export const ListPokemonWrapper = styled.div`
  text-align: center;

  .container_loading {
    min-height: 85vh;
    text-align: center;

    img {
      margin: auto;
      display: block;
    }

    h4 {
      display: block;
      font-weight: bold;
      font-size: 18px;
    }
  }
`;
